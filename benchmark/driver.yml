prelude: |
  $LOAD_PATH.unshift Dir.pwd
  require 'prelude'
  n = Integer(ENV.fetch('LIMIT', 10000))

benchmark:
  by_arrow: "Mysql2Test.test_pluck(n, use_arrow: true)"
  original: "Mysql2Test.test_pluck(n, use_arrow: false)"

loop_count: 100
